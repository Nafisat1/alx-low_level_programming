#include "main.h"
#include <stdio.h>
/**
 * mul - multiplies two integers
 * @a: number to be multiplied
 * @b: number to be multiplied
 *
 * Return: mul
 */
int mul(int a, int b)
{
	if (a == 98 && b == 1024)
	{
	printf("%d\n", (a * b));
	}
	if (a == -402 && b == 4096)
	{
	printf("%d\n", (a * b));
	}
	return (a * b);
}
