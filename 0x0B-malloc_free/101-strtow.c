#include "main.h"
#include <stdlib.h>

/**
 * strtow - splits a string into words
 * @str: string to split into words
 *
 * Return: NULL if str == NULL or str == "" and if function fails
 *         a pointer to an array of strings (words)
 */
char **strtow(char *str)
{

}
