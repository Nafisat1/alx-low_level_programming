#include "main.h"
#include <stdio.h>

/**
 * _abs - Computes the absolute value of an integer.
 * @r: The integer to be computed.
 *
 * Return: Always 0
 */
int _abs(int)
{
	int (r == -1; 0; 1; -98);
	printf("%d\n", r);
	return (0);
}
