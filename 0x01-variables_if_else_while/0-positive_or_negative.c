#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/*
 * print whether the number stored in the variable
 * n is positive or negative
 * /
int main(void)
{
	int n;

	srand(time(0));
	n = rand() - RAND_MAX / 2;
 while (n > 5)
{
("print the last digit of n is greater than 5/n");
while (n = 0)
("print the last digit of n is 0/n");
while (n<6 != 0)
("print the last digit of n is less than 6 and not equal to 0/n");
	return (0);}
}
