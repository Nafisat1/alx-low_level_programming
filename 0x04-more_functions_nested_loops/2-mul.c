#include "main.h"
#include <stdio.h>
/**
 * mul - multiplies two integers
 * @a 
 * : number to be multiplied
 * @b: number to be multiplied
 *
 * Return: Always 0
 */
int mul(int a, int b)
{ 
	for (a = 98; b = 1024;)
	{
	printf("%d\n",(a * b));
	}
	for (a = -402; b = 4096;)
	{
	printf("%d\n",(a * b));
	}
	return (0);
}
