#include "main.h"
#include <stdio.h>

/**
 * _abs - Computes the absolute value of an integer.
 * @r: The integer to be computed.
 *
 * Return: Always 0
 */
int _abs(int)
{
	int r;
	if (r == -1)
	{
		printf("%d\n", r);
	}
	else if (r == 0)
	{
		printf("%d\n", r);
	}
	else if (r == 1)
	{
		printf("%d\n", r);
	}
       else 
	{
	printf("%d\n", r);
	}
	return (0);
}
