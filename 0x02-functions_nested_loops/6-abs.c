#include "main.h"
#include <stdio.h>

/**
 * _abs - Computes the absolute value of an integer.
 * @i: The integer to be computed.
 *
 * Return: Always 0
 */
int _abs(int i)
{
	if (i >= 0)
	{
		return (i)
	}
	else
	{
		return (i * -1);
	}
}
